spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true

logging:
  level:
    com.ptit.tour: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
